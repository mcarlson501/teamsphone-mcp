id: diagnose-user-voice
version: 1.0.0
summary: Diagnose why a user cannot use Teams Phone by checking licensing, enterprise voice, number assignment, voice routing, dial plan, and emergency location in remediation order.
category: read
riskTier: 0
telephonyModels: [callingPlans, operatorConnect, directRouting]
annotations:
  readOnlyHint: true
  destructiveHint: false
  idempotentHint: true
inputs:
  tenantId: { type: string, required: true }
  credentialRef: { type: string, required: true }
  userUpn: { type: string, format: upn, required: true }
redactParams: []
maxBlastRadius: 0
timeoutSeconds: 120